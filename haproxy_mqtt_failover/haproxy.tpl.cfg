global
    daemon
    maxconn 256

defaults
    mode tcp
    timeout connect 5s
    timeout client  30s
    timeout server  30s

frontend mqtt_in
    bind *:${PORT}
    default_backend mqtt_cluster

backend mqtt_cluster
${BROKER_SERVERS}
