global
    log stdout format raw local0
    maxconn 2000

defaults
    log     global
    mode    tcp
    option  tcplog
    timeout connect 5s
    timeout client  30s
    timeout server  30s

frontend mqtt
    bind *:${PORT}
    default_backend mqtt_backend

backend mqtt_backend
    option tcp-check
${BROKER_SERVERS}
